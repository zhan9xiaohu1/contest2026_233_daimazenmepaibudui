/****************************************************************************
 * board/contest_board/src/sf32lb52_audio_in.h
 *
 * SF32LB52 录音通路封装（麦克风输入）
 *
 * 把「打开 / 配置 / START / read / STOP / close」这一套标准 NuttX audio
 * 接口收进四个函数，给不熟悉驱动的上层（成员二的 ai_audio.c 等）直接用。
 * 其中 stop() 与 abandon() 是两个**用途不同**的收尾入口，分工见各自的说明。
 *
 * 底层设备是 /dev/audio/audio0（注意带 audio/ 子目录），接口本身是
 * ioctl(AUDIOIOC_CONFIGURE, AUDIO_TYPE_INPUT) + ioctl(AUDIOIOC_START)
 * + read() + ioctl(AUDIOIOC_STOP)，已在真机 `hw_test audio 2` 验证
 * （read 返回 64000/64000 字节）。完整说明见
 * docs/audio_driver_usage.md 第 9 节。
 *
 * 与设备驱动本身的坑（低频次说明，细节见文档第 3.2、8 节）：
 *   - read() 会一直阻塞到读满；被 STOP 打断 / 设备没在跑时返回 0（EOF），
 *     等不到数据（下层那一次等待超时）则返回负值（errno = ETIMEDOUT）——
 *     两种语义不同，0 要收摊、超时要跳过这一帧接着读；
 *   - 下层一次 read 内部有 5 秒上界（一次等满，不分片），
 *     所以单次 read 不要超过 1 秒的数据量；
 *   - AUDIOIOC_STOP 会同时停掉播放和录音两条通路，不能全双工；
 *   - close() 在"最后一个 fd"时会走驱动的 shutdown 路径（关中断上下文），
 *     该路径已修（sf32lb52_audio.c 的最小化 hw_shutdown），但本模块
 *     绝不在里面做额外的事，只 ioctl(STOP) + close()
 *     （abandon() 是唯一的例外：它故意不发 STOP，见它那一段）。
 *
 ****************************************************************************/

#ifndef __BOARDS_CONTEST_BOARD_SRC_SF32LB52_AUDIO_IN_H
#define __BOARDS_CONTEST_BOARD_SRC_SF32LB52_AUDIO_IN_H

/****************************************************************************
 * Included Files
 ****************************************************************************/

#include <nuttx/config.h>
#include <nuttx/compiler.h>

#include <sys/types.h>
#include <stdint.h>

/****************************************************************************
 * Pre-processor Definitions
 ****************************************************************************/

/* 设备路径：驱动里是 audio_register("audio0")，上层会自动拼 /dev/audio/ 前缀 */

#define AUDIO_IN_DEV          "/dev/audio/audio0"

/* 本板录音固定用 16 kHz / 单声道 / 16bit（s16le）。
 * 16k 单声道 16bit = 每秒 32000 字节。 */

#define AUDIO_IN_DEFAULT_RATE 16000
#define AUDIO_IN_DEFAULT_BITS 16

/* 单次 read 的建议上限：1 秒的数据量。
 * 驱动下层一次 read 内部只等 5 秒（见 sf32lb52_audio.c 的
 * SF32LB52_AUDIO_RX_READ_TIMEOUT_MS），
 * 拆成 1 秒一块是最稳的（也是 audio_test 真机验证过的大小）。 */

#define AUDIO_IN_CHUNK_BYTES  (AUDIO_IN_DEFAULT_RATE * 2)

/****************************************************************************
 * Public Types
 ****************************************************************************/

/* 录音（RX）通路的只读计数快照，sf32lb52_audio_rx_stats() 的输出。
 *
 * 六个计数器都是**开机以来单调递增**的 32 位量（只看趋势，不做回绕保护：
 * 真跑到回绕那件事早该查完了）。它们存在的唯一目的是断流时**分诊**，六个数
 * 一起看才分得清坏在哪一段上：
 *   irq       RxCplt（完成）中断次数 —— 一次 read 的整块采满一次，也就是
 *             "上层要的那一帧到手了"；
 *   half      RxHalfCplt（半满）中断次数 —— 只是一记心跳（半满时不交数据、
 *             也不叫醒读者），用来分辨"数据在流没有"：**两个都不涨 = 数据源头
 *             真停了**（ADC/AUDPRC 那一侧）；irq 不涨而 half 涨 = 数据在流、
 *             只有完成通知那一路丢了；
 *   read      read() 进入次数（在涨说明上层一直在要，不是上层不读了）；
 *   timeout   read() 等待超时次数 —— 那一次 5 秒预算用光一次算一次；
 *   dma_err   DMA 传输错误(TE) 中断次数 —— HAL 在中断里就把通道拆了，
 *             它一涨就是那个指纹（read 会立刻收尾并按 -ETIMEDOUT 返回，
 *             不会空转满 5 秒）；
 *   lost      恒为 0（驱动没有"环里来不及取、被 DMA 覆盖"这回事，留着这个字段
 *             是为了不动上层的读取代码）。
 *
 * armed / busy 不是计数，是"此刻"的两个状态位 —— 断流那一刻这一对最有用：
 * armed=1 且 busy=1（DMA 挂着、也确实有个 read 在等）配上 irq/half 不涨，
 * 就是"通路武装得好好的、就是不来数据"。
 * （armed 就是"RX 通道此刻武装着"：read 武装成功到收尾之间为真。）
 *
 * 这几个数只由中断和 read 自己维护，本结构体只是**拷一份出来**；
 * 拿到的值可能差一两个中断的时间，诊断要的是"现在大概什么形状"。 */

struct sf32lb52_audio_rx_stats_s
{
  uint32_t irq;       /* 完成中断（RxCplt）次数 */
  uint32_t half;      /* 半满中断（RxHalfCplt）次数 */
  uint32_t read;      /* read() 进入次数 */
  uint32_t timeout;   /* read() 等待超时次数 */
  uint32_t dma_err;   /* DMA 传输错误(TE) 中断次数 */
  uint32_t lost;      /* 恒 0（见上面那段） */
  int      armed;     /* RX 通道此刻已武装 = 1，没有 = 0（口径见上面那段） */
  int      busy;      /* 此刻有 read 正等着数据 = 1，没有 = 0 */
};

/****************************************************************************
 * Public Function Prototypes
 ****************************************************************************/

/****************************************************************************
 * Name: audio_in_start
 *
 * Description:
 *   打开 + 配置 + 启动录音通路。成功返回 0。
 *
 *   **重复调用**：本线程已经在录音（板级记录里的持有者就是当前线程）→ -EBUSY；
 *   持有者还活着（别的线程/app 真在录）→ -EBUSY，板级不动那个会话。
 *   确证是残留（持有线程查不到 TCB；**且**要么这就是本组自己的残局，要么起会话
 *   的整个任务组也查不到 TCB —— 后者等价于"那个 app 没了"）→ 判为残留：
 *   丢掉记录后当成全新一次会话重开。
 *   要重录请先 audio_in_stop()。
 *
 *   **遇 EBUSY 怎么办（本次改动，原来是"绝不先停再开"）**：
 *     会先停再开（只做一次）的条件 —— 占用者被**判定为残留**。判定分两层，
 *     都在证据所在的那一层做：
 *       · 板级这一层：自己的记录（fd + 持有线程）里持有线程已消失，**并且**它那个
 *         任务组也查不到 TCB（= 那个 app 整个没了；本组自己的残局是例外，留着自愈）
 *         → 丢掉记录按全新会话重开，连 STOP 都不用发；
 *         "线程没了但那个 app 还活着（跨组）"不算残留，返回 -EBUSY 不去抢 ——
 *         那种会话是对方自己留在原地的（ai_audio.c 的 stale 收尾只退出、不 stop
 *         不 close），抢过来就是同一台半双工设备上两个 read 客户端：驱动 priv 是
 *         单实例，rx_sem / rx_busy / DMA 武装都只有一份，配对散掉后真机落在那条
 *         sem_waitirq.c:137 断言上。取舍与下面驱动那一层"起会话的线程死了、但
 *         app 还活着"完全一致；
 *       · 驱动那一层：方向冲突时驱动会查自己记下的持有者，判"残留"要三条同时成立
 *         —— 起会话的线程、最近读写的线程都查不到 TCB；**且**起会话的任务组也查不到
 *         TCB（NuttX 里组长的 pid 就是任务组，这条等价于"整个 app 没了"）；**且**
 *         这条会话已经起过至少 3 秒（SF32LB52_AUDIO_SWITCH_GRACE_MS，用于掐掉
 *         "会话刚起来、读数据的线程还没进第一次 read"的误伤窗口）。三条都满足才把
 *         那条残留通路干净 stop 掉再把方向切过来，对上层**透明**（START 返回 0）。
 *         注意驱动这一层的取舍：**"起会话的线程死了、但 app 还活着"不算残留**。
 *         本板真有"起会话的线程是短命线程"的用法（robot_ui 用 DETACHED 线程开麦、
 *         ai_audio 在播放线程里恢复录音后该线程退出），这种线程一退出、真正读数据的
 *         线程还没进第一次 read 的那一小段时间里两条线程证据都是空的，可会话是活的。
 *         所以这种情形宁可回 -EBUSY（那个 app 手里有 fd，由它自己的 STOP/close
 *         收尾），也不去替它强停一条正在录音/正在播报的活会话。
 *     绝不打断的条件 —— 占用者还活着（半双工下录音/播放必须串行）：驱动直接回
 *     -EBUSY，本函数**不发 AUDIOIOC_STOP、不先停再开**，只把 AUDIOIOC_START
 *     重试**一次**（一次为限，不循环、不 sleep），接住"对面刚好在这一拍收尾"的
 *     情况；仍然失败就如实返回 -EBUSY 并把原因打进日志。理由：AUDIOIOC_STOP 会
 *     同时停掉录和放，停在别人正在出声的播报上就是打断它，而板级这一步拿不出任何
 *     "对面已死"的证据（自己的会话记录在能走到 START 时一定是"无会话"），所以
 *     不能拿别人的播报去赌。
 *
 * Input Parameters:
 *   sample_rate - 采样率，只支持驱动支持的档位：
 *                 8000 / 16000 / 44100 / 48000（本板用 16000）；
 *   channels    - 声道数，目前只支持 1；
 *   bits        - 位深，目前只支持 16（s16le）。
 *
 * Returned Value:
 *   成功返回 0；参数非法返回 -EINVAL；open/ioctl 失败返回负 errno；
 *   设备仍被活着的会话占着（含重试一次之后仍然如此）返回 -EBUSY。
 *
 ****************************************************************************/

int audio_in_start(int sample_rate, int channels, int bits);

/****************************************************************************
 * Name: audio_in_read
 *
 * Description:
 *   读一段 PCM 进 buf（最多 len 字节），阻塞到读满、被 STOP 打断或下层超时。
 *
 *   **单次调用不要超过 AUDIO_IN_CHUNK_BYTES（1 秒 = 32000 字节）**：
 *   要录更长得自己循环调这个函数。
 *
 *   返回值的三种语义**必须分开处理**（本函数原样转发下层驱动的结果）：
 *     > 0 = 真读到了这么多字节；
 *     = 0 = EOF：被 AUDIOIOC_STOP 打断 / 设备没在跑 / 会话换代 ——
 *           这一代会话已经结束了，要跳出循环、把麦克风还回去；
 *     < 0 = 错误或超时。其中 errno = ETIMEDOUT 是**下层那一次等待超时**
 *           （驱动等满自己的 5 秒上界都没等到 DMA 完成）：会话还活着，只是这一帧
 *           没数据，调用方应当跳过这一帧接着读，只有连续超时到上限才当会话
 *           死了处理；其余负值（如未 start 的 -EINVAL）才是真的不能再读了。
 *   注意判"是不是超时"要看 errno（下层驱动返回的负值经 POSIX read() 那层
 *   会被翻成 -1 + errno），而且调用前先把 errno 清 0。
 *
 *   注意：本函数**不持锁**读（否则另一个任务就没法用 audio_in_stop()
 *   发 STOP 来把阻塞的 read 唤醒），只会在开头短暂取锁拿 fd 快照。
 *
 * Returned Value:
 *   实际读到的字节数；0 = EOF；< 0 为负 errno（未 start 时返回 -EINVAL，
 *   下层超时时是 -1 且 errno = ETIMEDOUT）。
 *
 ****************************************************************************/

ssize_t audio_in_read(FAR void *buf, size_t len);

/****************************************************************************
 * Name: audio_in_stop
 *
 * Description:
 *   停止并关闭录音通路（ioctl(AUDIOIOC_STOP) + close(fd)）。
 *   没在录音时是安全的空操作，返回 OK。
 *
 *   ⚠️ 本函数**发的是设备级 STOP**（同时停录和放）。如果这次会话"
 *   根本不是本层停的"、只是想把本层的句柄还回去（典型：audio_in_read() 返回
 *   0/EOF 之后的收尾），请用 audio_in_abandon() —— 那时设备多半已经被别人的
 *   会话接手，一个设备级 STOP 打过去就是把接手方打死。
 *
 *   可以从**同一个 app 的另一个线程**调用来"救"一个正阻塞在
 *   audio_in_read() 里的任务（驱动已修：STOP 能唤醒阻塞中的 read，read 返回 0）。
 *   task_create() 出来的子任务（hw_test 的读任务就是）虽然属于另一个 task group，
 *   但它的 fd 是**复制父任务 fd 表**得来的、指向同一个 file 对象，同样合法。
 *
 *   ⚠️ 边界：fd 号只在"开它的那个 task group"里保证有意义。别的 app（各自 exec
 *   起来的独立程序）拿着这个数字去 ioctl/close，只会打到它自己组里的另一个
 *   file 对象上 —— 真机事故就是这样：AUDIOIOC_STOP failed: 25（ENOTTY，音频
 *   驱动压根没收到命令），而原来紧接着还会 print "stopped" 并 close 掉那个无关
 *   fd，于是持有者的录音线程一直卡在 read() 里、300ms 收不了尾。
 *   现在的行为：跨组调用先做一次只读的身份探测（GETCAPS(QUERY)），
 *     - 探测不过 → 返回 -EPERM，**STOP 不发、fd 不关、全局状态不动**；
 *     - 探测通过（就是复制来的同一个音频设备）→ 照常停，日志里留下一行 WARNING；
 *     - 本组自己的调用 → 直接停。
 *   而且只有 STOP **成功**之后才 close/清全局；STOP 失败时什么都不动（设备可能
 *   仍在跑，持有者可以重试），错误原因和双方 group/线程 id 都打进日志。
 *   跨 app 要让路请改成"登记请求 + 持有者自己的线程执行"。
 *
 *   提醒：如果本模块是设备上**唯一**的使用者，close() 会走驱动的
 *   shutdown 路径；该路径曾在关中断上下文里卡死整机，现已修成最小化
 *   的 hw_shutdown()（见 docs/audio_driver_usage.md 第 8 节）。
 *
 *   还有第三种"OK"：本次会话已经有别的线程拿到收尾权正在收（共用标志），本函数
 *   确认那个线程还在之后**直接返回 OK，既没发 STOP 也没关 fd** —— 这是故意的
 *   （两个线程各 close 一次会关掉这个槽位刚被别人复用的 fd）。返回值看不出这一
 *   区别，所以这种情况会留一条限频 WARNING（每 3 秒最多一条，见 .c 里的
 *   AUDIO_IN_STOP_SPIN_LOG_MS），排查时按"stop 空转"搜。
 *
 *   **不要在中断上下文里调用**（close 路径可能取锁）。
 *
 * Returned Value:
 *   OK：正常停掉（或本来就没在录，或有别的线程正在收尾——后者见上面那段）；
 *   -EPERM：跨组调用且 fd 不是音频设备，本次没有动设备；
 *   -errno：AUDIOIOC_STOP 失败（设备可能仍在跑，日志里有明细）。
 *
 ****************************************************************************/

int audio_in_stop(void);

/****************************************************************************
 * Name: audio_in_abandon
 *
 * Description:
 *   放弃当前录音会话：只清理本层自己的状态并关闭 fd，**绝不发 AUDIOIOC_STOP**。
 *   用在「会话不是被本层停的」收尾路径上（read 返回 0/EOF 等），
 *   避免把一个设备级 STOP 打到别人正在用的会话上。
 *   返回 0 成功；负数表示本层当时本来就没有会话（已幂等）。
 *
 *   ── 什么时候用 abandon / 什么时候必须用 stop ──
 *     · 用 abandon —— 这一代会话**已经结束**，本层只是要还回自己的句柄：
 *       audio_in_read() 返回 0/EOF 之后的收尾就是这种情况。EOF 的三个来源
 *       （见 audio_in_read）分别是"被 AUDIOIOC_STOP 打断 / 设备没在跑 /
 *       会话换代"，前两个说明设备已经不在跑这一代了，第三个说明设备已经被
 *       别人的会话接手。两种情况下再发 STOP 都是纯破坏：AUDIOIOC_STOP 是
 *       **设备级**的，会同时停掉播放和录音，于是"被接手方"的收尾动作会把刚
 *       接手设备的那个会话打死 —— 单向让路变成双向拆台。
 *     · 必须用 stop —— 会话还是本层的、需要**主动把设备停掉**时：录够了、
 *       上层要切去播放（半双工必须串行）等等。只有 STOP 是"停"，
 *       也只有 STOP 能唤醒另一个阻塞在 audio_in_read() 里的线程。
 *
 *   ── 为什么"只关自己的 fd"就够，不会把设备永久锁住 ──
 *     close() 走到 NuttX 音频上层 audio_close()（nuttx/audio/audio.c:223-283），
 *     当这是该设备**最后一个**还开着的 file 对象时，上层会调
 *     lower->ops->shutdown()，也就是驱动的 sf32lb52_audio_shutdown() →
 *     sf32lb52_audio_hw_shutdown()（board/contest_board/src/sf32lb52_audio.c:1514）：
 *     那里停 DMA、DISABLE AUDPRC、软复位、关功放、nxsem_post() 唤醒可能还在
 *     read() 里等的人，并把 priv->running 清成 false、把三个持有者 id 清成 -1。
 *     所以"不发 STOP 就还回去"不会留下一个下不来台的 running：本层会话状态在
 *     最后一个 fd 被关时就被清干净了，下一个 app 可以正常 START。
 *     反过来，只要还有别人开着这个设备，上层就**不会**调 shutdown —— 这正是
 *     本函数想要的效果：别人的会话一个字节都不动。
 *
 *   ── 边界（用错会出问题，务必看） ──
 *     · abandon **不唤醒**任何阻塞中的 read：它没有 STOP 可发。如果本层还有
 *       线程卡在 audio_in_read() 里，要么等它自己出来，要么改用 stop()。
 *       另外 NuttX 的 close() 在"同一个 file 对象还被别的线程用着"（比如那次
 *       阻塞的 read 还持有引用）时只先减引用计数，真正的 close（以及上面的
 *       上层 shutdown）要等那次 read 返回后才执行
 *       （fs/inode/fs_files.c 的 file_put）。所以别把 abandon 当"另一种 stop"
 *       用在需要**立刻**把设备停下来的场景。
 *     · 跨 task group 的调用沿用 stop() 那套只读身份探测（GETCAPS(QUERY)）：
 *       探测不过就返回 -EPERM，fd 不关、全局不动；探测通过（task_create
 *       复制出来的同一个 file 对象）才照常关，并留一行 WARNING。
 *     · 与 stop() **共用**同一个"正在收尾"标志：两个线程同时收同一次会话时，
 *       后到的那个直接返回，不会出现两次 close 打到同一个 fd 槽位上。
 *     · 不要在中断上下文里调用（close 路径可能取锁）。
 *
 * Returned Value:
 *    OK      —— 已放弃（fd 已关，没发 AUDIOIOC_STOP）；
 *    -ENOENT —— 本层当时没有会话：没 start 过 / 已经收过尾 / 已有别的线程
 *               （stop 或另一次 abandon）正在收这一次会话。这是**幂等返回，
 *               不是错误**，调用方按"已经干净了"处理即可；
 *    -EPERM  —— 跨 task group 调用且那个 fd 号在本组里不是音频设备：
 *               什么都没动（会话仍在原持有者手里）；
 *    -EINVAL —— 锁坏掉了（不可能的状态）。
 *
 ****************************************************************************/

int audio_in_abandon(void);

/****************************************************************************
 * Name: sf32lb52_audio_rx_stats
 *
 * Description:
 *   把录音（RX）通路的六个计数器 + armed/busy 两个状态位**只读地**拷给调用方
 *   （口径见上面 struct sf32lb52_audio_rx_stats_s 的注释）。它是纯观测入口：
 *   不加锁、不碰设备、不 post 任何信号量、不读任何硬件寄存器，只读驱动里几个
 *   32 位变量就返回 —— 所以**任何线程**都能调（现在用的是 hello_app 的主循环
 *   和 robot_ui 的 MQTT 收包线程，后者一阻塞，唯一的观测通道就一起没了）。
 *
 *   为什么要有它：录音线程"卡在 read 里、标志却全健康"那种断流（ract=1、
 *   上层 idle 一路涨），光看上层分不清是"卡在无界等待"还是"DMA 交帧那一半
 *   丢了"。这六个计数器正是驱动内部那两条可能性的分界：irq/half 还在涨 =
 *   数据在流、是等待那一侧的问题；一个都不涨而 read 在涨 = 请求线/ADC 侧死了；
 *   dma_err 在涨 = TE（那一类不会自愈）；lost 在涨 = 上层自己来不及取。
 *
 *   ⚠️ 它**不改变任何行为**：不唤醒、不恢复、不重置计数器（计数是"开机以来"
 *   的口径，谁都不许把它清零）。恢复动作属于录音链路自己的那条路，不属于观测。
 *
 * Input Parameters:
 *   stats - 调用方给的结构体，本函数把当前值写进去（调用前内容无所谓）。
 *
 * Returned Value:
 *   OK      —— 已填好（各字段见上面那个结构体）；
 *   -EINVAL —— stats == NULL；
 *   -ENODEV —— 音频驱动还没初始化（/dev/audio0 还没注册，hello_app 可能还
 *              没起来）。此时 stats 一个字节都不写，调用方按"取不到"处理。
 *
 ****************************************************************************/

int sf32lb52_audio_rx_stats(FAR struct sf32lb52_audio_rx_stats_s *stats);

#endif /* __BOARDS_CONTEST_BOARD_SRC_SF32LB52_AUDIO_IN_H */
